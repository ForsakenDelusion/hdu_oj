#include "bits/stdc++.h"

using namespace std;

bool is_

int main() {
    int year;
    int month;
    int day;
    while (scanf("%d/%d/%d", &year, &month, &day) != EOF) {
        
    }
    return 0;
}